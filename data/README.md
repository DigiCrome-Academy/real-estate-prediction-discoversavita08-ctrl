# Data Directory

This directory is used for storing downloaded datasets.
The California Housing dataset is loaded directly via scikit-learn,
so no manual download is required.

If you use the Kaggle dataset, place the CSV files here.
