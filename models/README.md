# Models Directory

Save your trained models here using `save_model()` from `src/ensemble.py`.

Example:
```python
from src.ensemble import save_model
save_model(my_model, 'models/best_ensemble.joblib')
```
